# Optional deps for distributed mode (Ray)

# Ray core
ray>=2.0

# DB (optional but recommended)
duckdb>=0.9

# For qNEHVI proposer
botorch>=0.9
# torch is intentionally NOT pinned here because you need to install the
# correct CUDA build for your GPU driver.
