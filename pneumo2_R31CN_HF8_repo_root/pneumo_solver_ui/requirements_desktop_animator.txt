# Дополнительные зависимости для Desktop Animator (Qt/OpenGL)
#
# Установка (в активированном venv):
#   python -m pip install -r pneumo_solver_ui/requirements.txt
#   python -m pip install -r pneumo_solver_ui/requirements_desktop_animator.txt
#
# Примечание:
#   3D-визуализация использует pyqtgraph.opengl, которому нужен PyOpenGL.

PySide6>=6.5
pyqtgraph>=0.13
PyOpenGL>=3.1
PyOpenGL_accelerate; platform_system == "Windows"
