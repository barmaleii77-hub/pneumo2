# RELEASE NOTES — R18 — 2026-03-19

## Что исправлено

- Исправлен Windows launcher: теперь он не доверяет только metadata-проверке пакетов в shared venv.
- Если metadata говорит "всё установлено", но import smoke показывает пропавшие `numpy/pandas/...`, launcher пересоздаёт shared venv и запускает полноценную переустановку зависимостей.
- Если после установки обязательные импорты всё ещё не проходят, запуск блокируется с явным сообщением об ошибке вместо тихого падения веб-интерфейса.
- Релиз пересобран в реально **Windows-clean** виде: удалены длинные nested report/cache пути, которые ломали распаковку через Windows Explorer.

## Что не изменилось

- Это всё ещё переходный релиз по линии R17 source-data/contract.
- Финальный перевод solver/animator на новые hardpoints в этот релиз не включён.
