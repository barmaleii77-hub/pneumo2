R31CN_HF8: the explicit objective/penalty contract is now persisted and enforced across distributed optimization. Coordinator run dirs write objective_contract.json, Optimization history shows objective stack + hard gate for each run, and stable problem_hash now changes when objective_keys / penalty_key / penalty_tol change so resume/cache do not silently mix different quality contracts.
