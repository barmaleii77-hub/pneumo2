# TODO_WISHLIST_R31Y_ADDENDUM_2026-03-24

## Закрыто в R31Y
- dense road surface перестаёт зависеть от viewport-local normal estimation;
- live 3D GL снова docked-by-default, old forced detached startup убран;
- old persisted detached-only layout gated новой layout version;
- manual stop playback немедленно возвращает panes из lite/perf-mode;
- confusing piston scatter markers скрыты по умолчанию;
- contract/docs обновлены под honest housing shell fallback.

## Остаётся открытым
- fresh Windows acceptance bundle на R31Y;
- explicit exporter-side gland/body-end contract для цилиндров;
- финальная проверка CPU tail после расчётов/stop playback;
- catalogue-aware Camozzi sizing/limits поверх базового packaging contract.
