# GUI_SPEC_ARCHIVE_LINEAGE_APPENDIX_V38

V38 не заменяет локальный канон `17/18`.  
Он добавляется как successor imported knowledge-base layer после `foundations/*` и до `v3/*`.

Исторические imported layers сохраняются:
- `v3` — active detailed historical reference;
- `v13_ring_editor_migration` — specialized `WS-RING` addendum;
- `v12_design_recovery` — recovery/provenance layer.

V38 не объявляет runtime closure и не закрывает producer-side gaps без evidence.
