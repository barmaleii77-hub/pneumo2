# TODO (после R02)

## 1) Явные ключи для самых важных виджетов
Хотя `ui_autokeys` закрывает проблему «пропадания», **явные** `key=` лучше:
- стабильнее при рефакторинге (не зависят от строки/файла),
- проще дебажить,
- проще навешивать специфические подсказки по ключам.

Приоритет:
- ключевые переключатели «модели/сценария/оптимизации»,
- важные поля путей (npz, csv, mapping),
- таблицы ввода (data_editor),
- настройки анимации/видеопанелей.

## 2) Единый «информационный слой» поверх страниц
Streamlit‑tooltip может быть незаметен. Нужен режим «подсказки крупно»:
- фиксированная панель «Что делать дальше»,
- список шагов,
- текущий статус (загружено/рассчитано/готово к анимации),
- явные предупреждения о недостающих данных.

## 3) Визуализация для быстрых выводов
Добавить в веб‑интерфейс (где логично):
- мини‑теплокарты (animated) по времени для давлений/расходов/скоростей,
- «стрелки» (векторные индикаторы) на ключевых узлах,
- статусы «норма/предупреждение/авария» по ISA‑подходу.

## 4) Контроль перегруженности таблиц
Где много чисел:
- по умолчанию показывать агрегаты/минимум колонок,
- давать кнопку «показать подробности»,
- добавлять графические маркеры (цвет/иконки) вместо чистых чисел.

## 5) Персистентность: миграции профиля
Сейчас формат `autosave_profile.json` имеет `_schema`. Нужен механизм миграции
между версиями (если переименуем ключи или структуру таблиц).

