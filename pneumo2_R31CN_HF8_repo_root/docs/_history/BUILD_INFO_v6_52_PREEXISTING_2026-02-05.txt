Build: v6.52 (Mechanika651v652)
Date: 2026-02-01

Changes (high level):
- Fix: автопроверки (autoverif) больше не штрафуют t_пересечения_Pmid_с=+inf (нет пересечения в пределах теста).
- Fix/UI: baseline-таблица теперь считает общий штраф как (штраф_цели + штраф_верификация) и показывает pass-флаги.
- Fix: ошибки компиляции временных рядов (road_csv/axay_csv) больше не проглатываются; по умолчанию это фатально (timeseries_strict=True).
- Fix: suite.json — строгий разбор JSON-полей (road_surface/params_override) + явные сообщения об ошибках.
- Fix: защита от дублей имён тестов в suite (иначе коллизии при агрегации метрик/кэше).
- UI: добавлена валидация дублей имён и JSON в редакторе тест-набора, и блокировка baseline при ошибках.
