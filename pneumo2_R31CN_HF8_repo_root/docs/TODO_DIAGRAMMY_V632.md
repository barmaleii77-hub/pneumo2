# TODO (Диаграммы V6.32)

Ниже список задач/идей, которые логично продолжать после этого релиза.

## A. GUI (Desktop Compare Viewer)
1. **Linked selection Web↔GUI:** экспорт/импорт «сеанса сравнения» (набор NPZ + выбранные сигналы + ref + режимы) через JSON.
2. **Click-to-jump (Heatmap → playhead):** в Qt heatmap уже можно выбирать run/signal; добавить выбор времени (time×signal) с прыжком на t.
3. **Auto-top-deltas:** кнопка «Top-N worst signals» (по max|Δ| или RMS) с автозагрузкой в список сигналов.
4. **Signal groups:** быстрые пресеты «Кузов», «Колёса», «Давления», «Клапана», «Расходы», «Углы», «Ускорения».

## B. Web
1. **Нормальная интерактивная «кликабельность»** для heatmap (Streamlit component / plotly events) чтобы клик по ячейке выбирал сигнал и ставил playhead.
2. **Parallel coordinates / scatter-matrix** для N→N анализа параметров/метрик (особенно для пакетов оптимизации).
3. **Экспорт отчёта** (HTML/PDF) по выбранному набору сравнений: графики + heatmaps + статистика.

## C. Данные/экспорт
1. Стандартизировать имена каналов для rel0 и unit-суффиксов.
2. Сохранение в NPZ «meta_numeric» как таблицы, чтобы influence работал без эвристик.

