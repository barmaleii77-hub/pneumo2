# WISHLIST_MASTER_v14 — R31G addendum (2026-03-22)

- Browser Performance trace exporter в SEND-bundle остаётся желательным: после R31G причины post-run CPU уже локализованы в коде, но acceptance должен опираться не на догадку, а на trace.
- Нужен единый JS helper для idle/viewport policy всех web-components, чтобы следующие патчи не дублировали одну и ту же логику по HTML-файлам.
- Для амплитуды во всех местах ввода/preview нужен единый словарь терминов: `A = полуразмах`, `p-p = 2A`.
