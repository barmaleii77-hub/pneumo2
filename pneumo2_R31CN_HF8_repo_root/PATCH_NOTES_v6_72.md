# PATCH_NOTES_v6_72

Дата (UTC): 2026-02-12 23:34:40Z

## P0 (восстановление доступа к функционалу / без потери возможностей)
- **Навигация Streamlit теперь включает `pneumo_solver_ui/pages_legacy/` без скрытых переключателей.**
  - Приоритет остаётся за `pages/` (если файл с таким же именем есть в `pages/`, legacy-дубликат не добавляется в меню).
  - Архивные дубликаты `__dup*` не добавляются в меню (но остаются на диске для истории).

## P1 (устойчивость инструментов)
- `pneumo_solver_ui/self_check.py`: добавлено гарантированное добавление путей в `sys.path`, чтобы динамически загружаемые модели могли импортировать `scheme_integrity` и др. при запуске через `python -m`.

## Не менялось
- Физика/модели/оптимизация: без изменений (только инфраструктурные фиксы мерджа/доступности).
- Источник истины схемы: без изменений в этом патче.
