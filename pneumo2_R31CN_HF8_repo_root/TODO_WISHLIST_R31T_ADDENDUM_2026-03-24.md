# TODO / WISHLIST addendum — R31T (2026-03-24)

## Done
- Bundle/view-stable road grid spacing added to Desktop Animator, closing the remaining R31S drift mechanism where cross-bar spacing still followed the instantaneous playback window.
- Auxiliary detached pane cadence floors raised to more visibly live values (`24/12`, `18/10`) without undoing lighter-overlay playback mode.
- `AnimatorAuxCadence` telemetry added so the next SEND bundle can prove detached pane redraw cadence quantitatively.

## Still open
- Live Windows retest on `R31T` with a fresh SEND bundle.
- Canonical `road_width_m` in export/meta.
- Measured browser/Windows perf acceptance.
- Solver-points completeness / cylinder packaging contract.
